import 'package:flutter/material.dart';

class FeedBackground extends StatelessWidget {
  const FeedBackground({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Feed Background'),
    );
  }
}
